/// A class to hold the metrics data for a course
class CourseMetrics {
  final int totalCards;
  final int completedBooks;

  CourseMetrics({
    required this.totalCards,
    required this.completedBooks,
  });
}